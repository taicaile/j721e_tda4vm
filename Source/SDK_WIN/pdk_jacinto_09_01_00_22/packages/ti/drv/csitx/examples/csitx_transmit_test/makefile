#
# This file is the makefile for building CSI-TX display test app for TI RTOS
#
ifeq ($(RULES_MAKE), )
include $(PDK_INSTALL_PATH)/ti/build/Rules.make
else
include $(RULES_MAKE)
endif


APP_NAME = csitx_transmit_testapp_$(BUILD_OS_TYPE)

include makefile.mk
